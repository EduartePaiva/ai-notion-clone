import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    // typed routes, I'll wait until it's not experimental
    // experimental: {
    //     typedRoutes: true,
    // },
};

export default nextConfig;
